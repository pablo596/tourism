import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { InfoConsejoPage } from './info-consejo';

@NgModule({
  declarations: [
    InfoConsejoPage,
  ],
  imports: [
    IonicPageModule.forChild(InfoConsejoPage),
  ],
})
export class InfoConsejoPageModule {}
